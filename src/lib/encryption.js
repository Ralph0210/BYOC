// Basic stub for encryption service
// In production, use Web Crypto API or a library like crypto-js
export const encrypt = async (text) => {
  if (!text) return text
  // MVP: No real encryption yet
  return text
}

export const decrypt = async (text) => {
  if (!text) return text
  // MVP: No real decryption yet
  return text
}
